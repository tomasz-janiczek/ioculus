<tr><td>
	{iScheme name=admintopbar title=$iScheme.attr.title}
	{/iScheme}

	{iScheme name=adminmenubar title=$iScheme.attr.title image=$iScheme.attr.image buttons=$iScheme.attr.buttons}
	{/iScheme}

	{iScheme name=table display=header}
	{/iScheme}
