<tr><td>
	{iScheme name=admintopbar title=$iScheme.attr.title}
	{/iScheme}

	{iScheme name=adminmenubar title=$iScheme.attr.title image=$iScheme.attr.image}
	{/iScheme}

	{iScheme name=menubig buttons=$iScheme.attr.buttons}
	{/iScheme}
